
Explicit waits
    await page.waitForSelector('tu_selector');
    await page.waitForFunction('tu_funcion');
    await page.waitForXPath('tu_xpath');
    await page.waitForUrl('http://www.url.com');
    await page.waitForTimeout(3000);

Implicit wait emulated
    async function esperaImplicita(milisegundos: number) {
        return new Promise((resolve) => {
            setTimeout(() => {
                resolve();
            }, milisegundos);
        });
    };

    await esperaImplicita(3000);

    o tambien se puede usar 
    await page.waitForTimeout(5000);
    

